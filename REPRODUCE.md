# 复现指南

本文档是 PairUAV-on-VGGT 实现的命令级复现入口。

计划中的最小主链:

1. 抽取冻结 VGGT 特征。
2. 构造与特征 cache 对齐的几何标签。
3. 训练连续角度头。
4. 训练独立距离头。
5. 执行连续 test 推理。
6. 可选执行 MAP-hard 后处理。

后续每新增或调整一个命令行入口,都需要同步检查这里的命令是否仍然匹配实际 CLI。
